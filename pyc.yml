language: ru
shell:
  exec: "bash"
alias:
  - чд: cd
  - пвд: pwd
  - уич: which
output:
  translate: true
prompt:
  prompt_line: "${KBOLD}${KYEL}${USER}${KRST} on ${KBOLD}${KGRN}${HOSTNAME}${KRST} in ${KBOLD}${KCYN}${WRKDIR}${KRST} ${KBOLD}${KMAG}${GIT_BRANCH} ${GIT_COMMIT}${KRST} ${KYEL}${CMD_TIME}${KRST}"
  history_size: 256
  translate: false
  break:
    enabled: true
    with: "❯"
  duration:
    min_elapsed_time: 2000
  rc:
    ok: "✔"
    error: "✖"
  git:
    branch: "on  "
    commit_ref_len: 8
    commit_prepend: "("
    commit_append: ")"
